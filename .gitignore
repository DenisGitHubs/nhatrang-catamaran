node_modules
dist
.DS_Store

# Инструменты Claude Code и внутренние заметки — не публикуем
.claude/
CLAUDE.md

# Мусор сборки / окружения
vite.config.js.timestamp-*.mjs
.vercel
Thumbs.db

# Секреты окружения — НИКОГДА не коммитим (шаблон в .env.example)
.env
.env.local
.env.*.local
